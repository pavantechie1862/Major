export class MessgeBody {
    message:string;
    messageType:boolean;
    constructor(message:string,messageType:boolean)
    {
        this.message=message;
        this.messageType=messageType;
    }
}